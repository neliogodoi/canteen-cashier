rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /canteens/{canteenId} {
      allow read, write: if request.auth != null;
    }

    match /canteens/{canteenId}/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
